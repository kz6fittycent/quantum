# Do not put test specific entries in this inventory file.
# For script based test targets (using runme.sh) put the inventory file in the test's directory instead.

[testgroup]
# quantum_python_interpreter must be set to avoid interpreter discovery
testhost quantum_connection=local quantum_python_interpreter="{{ quantum_coupling_python }}"
